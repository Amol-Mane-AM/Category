import { Category } from './category.mode'; // Ensure Category is exported as a class or value from category.mode.ts

describe('Category', () => {
  it('should create an instance', () => {
    //expect(new Category()).toBeTruthy();
  });
});
