export interface Category {

  // define properties here if needed
  // properties
    
  categoryName: string;
  description: string;
  status: boolean;
  categoryId?: number; // Optional property
   
}